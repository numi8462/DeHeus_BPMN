# CLAUDE.md

## 프로젝트 개요

이 프로젝트는 예전에 진행했던 그룹 프로젝트를 포크해와서 다시 완성시키는 **복원 작업**이다.
새로 만드는 프로젝트가 아니라, 원래 있던 코드베이스를 되살려서 다시 돌아가게 만드는 것이 목표다.

- BPMN 다이어그램 툴 웹 애플리케이션
- 핵심 라이브러리: **bpmn.js**
- 프론트엔드: **React** + **Tailwind CSS**
- 구조: `client/`, `server/` 폴더 분리
- 서버 배포: 예전에 **Docker**로 배포했었음
- 서버 백엔드 기술 스택: 확인 안 됨 → 작업 시작 전 `server/` 폴더의 `package.json` / `requirements.txt` 등 설정 파일을 직접 확인하고 파악할 것
- 패키지 매니저: **npm**

## 왜 수정이 필요한가

예전에 사용하던 API 키, 환경 변수, 배포 관련 키 등이 지금은 대부분 만료되었거나 유효하지 않다.
따라서 파일 전반을 검토하면서 오래되거나 깨진 설정, 의존성 버전 문제, 사용 불가능한 키 참조 등을 찾아 수정해야 한다.

**중요: API 키/시크릿/토큰 등은 절대 임의로 값을 만들어내거나 추측해서 채우지 않는다.**

- `.env` 등에서 만료되었거나 비어있는 키를 발견하면, 어떤 키가 필요한지 정리해서 사용자에게 알리고 새로 발급받도록 요청한다.
- 더미 값이나 예시 값을 실제 키인 것처럼 넣지 않는다.

## 작업 방식

1. 먼저 `client/`, `server/` 전체 구조와 주요 설정 파일(`package.json`, `.env.example`, `Dockerfile`, `docker-compose.yml` 등)을 훑어보고 현재 상태를 파악한다.
2. 실행/빌드를 시도하면서 에러를 마주치면:
   - **먼저 스스로 원인을 분석하고 해결을 시도한다.** (의존성 버전 문제, 코드 문법 오류, deprecated API 사용 등은 직접 고친다)
   - 정말 판단이 필요한 부분(비즈니스 로직 해석이 애매한 경우, 여러 방식 중 선택이 필요한 경우, 키/크리덴셜이 필요한 경우 등)만 사용자에게 질문한다.
   - 사소한 에러 하나하나 보고하지 말고, 필요한 질문만 모아서 물어본다.
3. 배포 환경(예: AWS/GCP/VPS 등)은 아직 정해지지 않았으므로, 배포 관련 설정을 임의로 특정 클라우드에 맞춰 바꾸지 않는다. 배포 관련 결정이 필요하면 먼저 물어본다.

## 코딩 원칙

### 1. 코딩 전에 먼저 생각하기

추측하지 말고, 헷갈리는 부분을 숨기지 말고, 트레이드오프를 드러낸다.

- 가정을 명확하게 밝힌다. 확실하지 않으면 질문한다.
- 여러 해석이 가능하면, 조용히 하나를 선택하지 않고 각각 제시한다.
- 더 단순한 접근이 있다면 말한다. 필요하면 반대 의견도 낸다.
- 무언가 불명확하면 멈추고, 무엇이 헷갈리는지 설명한 뒤 질문한다.

### 2. 단순함 우선

문제를 해결하는 최소한의 코드만 작성한다. 추측 기반 구현은 하지 않는다.

- 요청하지 않은 기능을 추가하지 않는다.
- 한 번만 쓰는 코드를 위해 추상화하지 않는다.
- 요청되지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 발생할 수 없는 상황을 위한 에러 처리를 추가하지 않는다.
- 과하게 복잡해 보이면 더 단순하게 다시 작성한다.

### 3. 외과 수술처럼 최소 변경하기

필요한 부분만 건드리고, 만든 흔적만 정리한다.

- 기존 코드/주석/포맷팅을 임의로 "개선"하지 않는다.
- 망가지지 않은 코드는 리팩터링하지 않는다.
- 기존 스타일을 따른다.
- 관련 없는 죽은 코드를 발견하면 언급만 하고 삭제하지 않는다.
- 단, 이번 변경으로 인해 안 쓰이게 된 import/변수/함수는 제거한다. (원래부터 있던 죽은 코드는 요청 전까지 건드리지 않는다)
